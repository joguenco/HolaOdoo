<odoo>
    <template id="products_category_web_portal" name="Products by Category">
        <t t-call="web.frontend_layout">
            <t t-set="title">
				Products
			</t>
            <div class="container">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a href="/">
								Inicio
							</a>
                        </li>
                        <li class="breadcrumb-item active" aria-current="page">
                            <t t-out="categorie_name" />
                        </li>
                    </ol>
                </nav>
                <h2 t-out="category.name" />

                <!-- <t t-call="web_portal_ii.website_search_box_input">
                    <t t-set="_classes" t-valuef="mt8" />
                    <t t-set="search_type" t-valuef="all" />
                    <t
                        t-set="action"
                        t-valuef="/hola/odoo/v2/products/category/{{slug(category)}}/search"
                    />
                </t> -->

                <div t-if="pager" class="o_portal_pager d-flex justify-content-center">
                    <t t-call="portal.pager" />
                </div>
                <table class="table">
                    <thead>
                        <tr>
                            <th>
								Name
							</th>
                            <th>
								Type
							</th>
                        </tr>
                    </thead>
                    <tbody>
                        <t t-foreach="products" t-as="product">
                            <tr>
                                <td>
                                    <t t-out="product.name" />
                                </td>
                                <td>
                                    <t t-if="product.type == 'consu'">
										Goods
									</t>
                                    <t t-if="product.type == 'service'">
										Service
									</t>
                                    <t t-if="product.type == 'combo'">
										Combo
									</t>
                                </td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </div>
        </t>
    </template>

    <!-- <template
        id="website_search_box_input"
        inherit_id="web_portal_ii.website_search_box"
        primary="True"
    >
        <xpath expr="//input[@name='search']" position="attributes">
            <attribute name="t-att-data-search-type">search_type</attribute>
            <attribute name="t-att-data-limit">limit or '5'</attribute>
            <attribute
                name="t-att-data-display-image"
            >display_image or 'true'</attribute>
            <attribute
                name="t-att-data-display-description"
            >display_description or 'true'</attribute>
            <attribute
                name="t-att-data-display-extra-link"
            >display_extra_link or 'true'</attribute>
            <attribute
                name="t-att-data-display-detail"
            >display_detail or 'true'</attribute>
            <attribute name="t-att-data-order-by">order_by or 'name asc'</attribute>
        </xpath>
        <xpath expr="//div[@role='search']" position="attributes">
            <attribute name="t-attf-class" remove="s_searchbar_input" separator=" " />
        </xpath>
        <xpath expr="//div[@role='search']" position="replace">
            <form
                t-attf-class="o_searchbar_form s_searchbar_input #{_form_classes}"
                t-att-action="action"
                method="get"
                t-attf-data-snippet="s_searchbar_input"
            >
                <t>$0</t>
                <input
                    name="order"
                    type="hidden"
                    class="o_search_order_by"
                    t-att-value="order_by if order_by else 'name asc'"
                />
                <t t-out="0" />
            </form>
        </xpath>
    </template>

    <template id="website_search_box" name="Website Searchbox">
        <div t-attf-class="input-group #{_classes}" role="search">
            <t t-set="search_placeholder">Search...</t>
            <input
                type="search"
                name="search"
                t-att-class="'search-query form-control oe_search_box %s %s' % (_input_classes or '', '' if default_style else 'border-0 bg-light')"
                t-att-placeholder="placeholder if placeholder else search_placeholder"
                t-att-value="search"
            />
            <button
                type="submit"
                t-att-class="'btn oe_search_button %s' % (_submit_classes or ('btn-primary' if default_style else 'btn-light'))"
                aria-label="Search"
                title="Search"
            >
                <i class="oi oi-search" />
                <span t-if="search" class="oe_search_found">
                    <small>(<t t-out="search_count or 0" /> found)</small>
                </span>
            </button>
        </div>
    </template> -->
</odoo>
